def fibo_recursivo(n):
    
    if n == 0:
        return 0
    if n == 1:
        return 1
    return fibo_recursivo(n-2) + fibo_recursivo(n-1)

n = int(input("Ingrese el ultimo numero de la serie fibonacci: "))
print(fibo_recursivo(n))
